(function ($, Drupal, window, document, undefined) {Drupal.behaviors.deadpool = {attach: function(context, settings) {
$('html').once(function(){
	// ------------------------------------- // VARIABLES
		jsRunCounter = 0;
	// ------------------------------------- // VARIABLES

  // ------------------------------------- // WINDOW READY
  // ------------------------------------- // WINDOW READY

  // ------------------------------------- // WINDOW FULLY LOADED
	  $(window).load(function() {
	  });
  // ------------------------------------- // WINDOW FULLY LOADED

  // ------------------------------------- // WINDOW RESIZED
	  $(window).resize(function() {
	  });
  // ------------------------------------- // WINDOW RESIZED

  // ------------------------------------- // WINDOW SCROLL
	  $(window).scroll(function () {
	  });
  // ------------------------------------- // WINDOW SCROLL
});
jsRunCounter++;
if (jsRunCounter > 1) {
//---------------------------------------- // AJAX COMPLETE

//---------------------------------------- // AJAX COMPLETE
};
}};})(jQuery, Drupal, this, this.document);